UNPRICE_TOKEN=unprice_test_xxx
BASE_URL=http://localhost:8787
PROJECT_ID=proj_xxx
CUSTOMER_ID=cus_xxx
EVENTS=1000

# Replay failure test.
# The API must run in a non-production APP_ENV for the failure trigger header to be honored.
REPLAY_AUTO_REPLAY=false
REPLAY_EVENT_SLUG=
REPLAY_POLL_INTERVAL_MS=1000
REPLAY_POLL_TIMEOUT_MS=60000
REPLAY_WINDOW_MS=300000
