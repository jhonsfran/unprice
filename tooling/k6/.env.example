UNPRICE_TOKEN=unprice_test_xxx
BASE_URL=http://localhost:8787
PROJECT_ID=proj_xxx
CUSTOMER_ID=cus_xxx
EVENTS=1000

# Budgeted runs test
BUDGET_AMOUNT=10000
CURRENCY=USD
EVENTS_PER_RUN=50
RUNS=10
VUS=5

# Latency benchmark (pnpm --filter @unprice/k6 latency)
# RATE=20
# DURATION=60s
# FEATURE_SLUG=tokens
# Cold path: creates real customers — disposable load-test projects only.
# COLD_SIGNUPS=20
# PLAN_SLUG=pro
