NODE features_usage_hourly_mv_node
SQL >
    SELECT
        toStartOfHour(fromUnixTimestamp64Milli(timestamp, 'UTC')) AS date,
        project_id,
        customer_id,
        period_key,
        feature_slug,
        argMaxState(value_after, timestamp) AS value,
        argMaxState(amount_after, timestamp) AS amount_after,
        argMaxState(currency, timestamp) AS currency
    FROM unprice_entitlement_meter_facts
    GROUP BY
        date,
        project_id,
        customer_id,
        period_key,
        feature_slug

TYPE MATERIALIZED
DATASOURCE unprice_feature_usage_hourly_mv
