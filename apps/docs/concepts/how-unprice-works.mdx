---
title: How Unprice works
description: "Where Unprice fits in your paid product path."
mode: wide
---

Unprice sits between your product, the paid work that your customer starts, and your payment
provider.

Your application owns the product experience and workload execution. Your payment provider captures
payment. Unprice owns the customer money path between them: pricing rules, entitlement decisions,
usage evidence, wallet credits, billing state, and the trail that explains why work was allowed,
denied, or billed.

<Frame caption="The money path: two requests accepted, the third denied.">
  <div className="w-full overflow-x-auto" tabIndex={0} role="region" aria-label="Money path diagram, scroll horizontally on small screens">
    <img className="block dark:hidden" src="/images/money-path.svg" width="880" height="660" style={{ minWidth: "660px", width: "100%", height: "auto" }} alt="Three $4.10 usage.consume requests share a $10 budget. Two are accepted and produce wallet, ledger, invoice, and Stripe payment evidence. The third is denied with LIMIT_EXCEEDED at $1.80 remaining, with no wallet change, invoice line, or charge." />
    <img className="hidden dark:block" src="/images/money-path-dark.svg" width="880" height="660" style={{ minWidth: "660px", width: "100%", height: "auto" }} alt="Three $4.10 usage.consume requests share a $10 budget. Two are accepted and produce wallet, ledger, invoice, and Stripe payment evidence. The third is denied with LIMIT_EXCEEDED at $1.80 remaining, with no wallet change, invoice line, or charge." />
  </div>
</Frame>

## The three boundaries

<CardGroup cols={3}>
  <Card title="Your product" icon="code">
    Runs the user experience and workload. It decides when a paid action is about to run.
  </Card>
  <Card title="Unprice" icon="route">
    Resolves pricing rules, entitlement decisions, budgets, wallet credits, billing state, and evidence.
  </Card>
  <Card title="Payment provider" icon="credit-card">
    Captures payment through Stripe or simulates payment in Sandbox.
  </Card>
</CardGroup>

## The money path

| Stage | What happens | Why it matters |
| --- | --- | --- |
| Paid action | Your app is about to run work that can create cost. | The money decision happens before the cost exists. |
| Pricing model | Unprice resolves the plan version, feature, meter, and entitlement. | Pricing logic stays out of product branches. |
| Runtime decision | Unprice checks entitlement, budget, wallet credits, and meter rules. | The request can be allowed or denied before work runs. |
| Evidence trail | Usage facts, wallet movement, ledger entries, and invoice evidence stay connected. | Support, analytics, replay, and invoices use one explanation. |
| Payment capture | Stripe captures payment or Sandbox simulates it. | The payment rail stays separate from pricing logic. |

## The payment-provider boundary

Unprice is not a Stripe wrapper. Stripe is the production provider today, and Sandbox is available
for testing. The provider interface separates invoice creation, payment methods, checkout and setup
sessions, payment collection, and webhooks from pricing and entitlement logic.

This separation lets Unprice keep the pricing and evidence path when the payment-capture system
changes. The provider model can support another provider without moving entitlement, metering,
wallet, billing, or invoice-evidence logic into product code.

## The runtime paths

<CardGroup cols={2}>
  <Card title="Ask without changing state" icon="shield-check" href="/libraries/ts/sdk/access/check">
    Use `access.check` when your app only needs to know whether a customer can use a feature.
  </Card>
  <Card title="Decide and apply now" icon="gauge" href="/libraries/ts/sdk/usage/consume">
    Use `usage.consume` when known usage must be denied after a limit, budget, or credit balance is exhausted.
  </Card>
  <Card title="Report usage evidence" icon="file-invoice-dollar" href="/libraries/ts/sdk/usage/record">
    Use `usage.record` when the request does not wait for Unprice to decide.
  </Card>
  <Card title="Reserve a workload budget" icon="route" href="/libraries/ts/sdk/runs/start">
    Use `runs.start`, `runs.consume`, and `runs.end` for jobs, workflows, tool chains, and agents.
  </Card>
</CardGroup>

## What Unprice does not own

Unprice does not run your product workload. Your application still calls the LLM, starts the job,
sends the API request, or runs the workflow.

Unprice also does not replace your payment provider. Stripe captures payment and Sandbox simulates
payment for testing. Unprice keeps the pricing logic and evidence path that explains what should be
charged, credited, denied, or replayed.
